{
    "name": "ipv/sistema-inspecciones",
    "description": "Sistema de Gestión de Inspecciones del IPV",
    
    "require-dev": {
        "phpunit/phpunit": "^10",
        "squizlabs/php_codesniffer": "^3.7"
    },

    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },

    "scripts": {
        "test": "phpunit --testdox",
        "lint": "phpcs --standard=PSR12 src"
    }
}